apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: aiops
spec:
  selector:
    app: redis
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30379
  # TODO: switch to ClusterIP once agent is deployed inside K8s
